# Local-only working docs (not tracked)
CLAUDE.md
docs/
README.md

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Editor
.vscode/

# Build artifacts (CUDA extension, later)
build/
*.so
*.egg-info/

# Transient benchmark logs / smoke-run CSVs (keep canonical result CSVs)
results/raw/*.log
results/raw/*_quick.csv
results/raw/*_quick.env.json

# Nsight binaries: large, regenerable via benchmarks/nsight_capture.sh.
# The *_cuda_gpu_kern_sum.csv summaries it emits ARE tracked.
results/profiling/*.nsys-rep
results/profiling/*.sqlite
